# 每日實習日誌 - 2026-08-05

## 今天在做什麼

今天校正 Skill 3 的評分模型，避免官方文件較完整的候選因「證據覆蓋率」被重複加分，並把 WorkSpaces AI Agents 的評估、文件與自動測試入口同步為同一個結論。

## 今天做了什麼

- Skill 3 rubric 改為五個固定構面：技術能力、證據可驗證性、導入前置條件、可控制性與停止機制、可逆性與終止；證據覆蓋率只能形成 review note 或 blocker，不再增加技術分數。
- WorkSpaces AI Agents 重新評估為 `2.65 / 5`：4、3、2、2、1。它未達 `3.75 / 5` PoC 門檻，且有 `compliance_review_required` blocker，因此 `recommend_poc=false`、`can_enter_skill4=false`。
- 釐清 WorkSpaces 的第一段 recipe 只驗證 CloudFormation、fleet、AgentAccessConfig 與短效 streaming URL；不開啟 URL、不連線 AI agent、不建立第二位串流使用者，也不證明 AI 已完成桌面工作流程。
- 第一段報價為低／預期／高 USD 0.05／0.10／0.40，建議核准上限 USD 0.50。完整桌面 agent session 是需另行核准的第二段，不與第一段混在同一筆核准。
- 同步專案首頁、dashboard、Skill 進度、WorkSpaces 摘要與 8/4 日誌，並加入 GitHub Actions，讓未來 push 與 PR 自動執行 Python 編譯與不依賴外部網站的完整單元測試；RSS 與即時 AWS 文章檢查保留為可選 integration test。

## 怎麼確認有做出來

- `python -m unittest tests.test_s3_s4 tests.test_s4_recipes tests.test_costing -v`：63 項通過。
- WorkSpaces Skill 3 artifact 顯示 `weighted_score=2.65`、`recommend_poc=false`、`can_enter_skill4=false` 與 `compliance_review_required`。
- 本次只更新程式邏輯、文件與自動化；未建立、修改或清除 AWS 資源。

## 對專案的幫助與分數

本次是評分模型與文件的一致性校正，不另外新增每日 Skill 分數；累積分數仍維持截至 2026-08-04 的 135 分。

## 問題與下一步

- WorkSpaces 若要重新考慮，需先處理合規覆核，並定義一個具體、受控且可清除的桌面任務；不能把「已有 recipe」理解為「可直接部署」。
- GitHub Actions 的第一個 push 會驗證離線單元測試是否可在乾淨環境重現；外部 AWS 網頁改版不應讓 CI 隨機失敗。
