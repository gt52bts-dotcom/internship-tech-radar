# 每日實習日誌 - 2026-07-22

## 今天在做什麼

今天把專案紀錄標準調嚴，也把 S3 Files PoC 清掉後重做成 CDK / CloudFormation 版。重點不是只讓部署成功，而是讓我看得懂資源怎麼串，之後可以自己再做一次。

## 今天做了什麼

- 重寫過去正式日誌的口吻，改成我自己的工作紀錄，不把 AI 操作寫成主體。
- 重新檢查 Skill 分數，把每日五個 Skill 加總上限改成 10 分，避免把成果寫太滿。
- 清掉昨天 S3 Files 手動 CLI 和 CloudFormation PoC 的 AWS 資源，避免 EC2、S3 Files、VPC 和 bucket 繼續花錢。
- 檢查 S3 bucket 和 CloudTrail。CloudTrail 判斷是帳號稽核用途，不是雷達專案或 S3 Files 殘留，不能隨便刪。
- 建立 S3 Files CDK PoC 專案與部署教學書，讓 CloudFormation 可以看到 bucket、S3 Files、mount target、access point、VPC、EC2 和 IAM 的關係。
- 細讀 CloudFormation 匯出的 YAML，每一段 resource 都對照用途和相依關係看過，確認自己不是只照指令部署。
- 修正教學書，把昨天手動 CLI 的核心步驟對應到 CDK 版：進 EC2、檢查 mount、讀檔、寫回 S3 都不能省。
- 自己照流程部署新的 CloudFormation stack。`cdk deploy` 因公司帳號 CDK bootstrap role 權限失敗，改用 `cdk synth` 加 `aws cloudformation deploy`。

## 怎麼確認有做出來

- 舊 S3 Files PoC cleanup 後，已用 CLI 回查，確認原本的 stack、S3 Files、EC2、bucket、IAM role、key pair、VPC 和 security group 不再存在。
- CloudTrail 用唯讀 CLI 查過，兩條 trail 都是帳號／組織層級的管理事件稽核，不是這次 PoC 建出來的資源。
- CDK PoC 本機 `synth` 可以產生 CloudFormation template，代表 CDK 程式語法和資源描述可用。
- 有對照 CloudFormation YAML、Infrastructure Composer 圖和教學書註解，理解 VPC、security group、S3 bucket、S3 Files、mount target、access point、EC2 和 IAM role 各自負責什麼。
- 新的 `s3files-cdk-202607221334` stack 已由 CLI 查到 `CREATE_COMPLETE`，Resources 裡 S3 bucket、S3 Files、mount target、access point、EC2、VPC 和 IAM 都是 `CREATE_COMPLETE`。
- 已手動進 CloudFormation Infrastructure Composer，看見 S3 Files、mount target、access point、VPC、EC2、security group 和 S3 bucket 的資源關係圖；截圖已保存為 PoC 證據。
- 這還不是完整 PoC 成功。下一步還要我手動進 S3 Files / EC2 / S3 Console 看狀態與物件，再進 EC2 確認 `/mnt/s3files` 和 S3 雙向檔案同步。

## 對專案的幫助與分數

| 項目 | 今日進度 | 分數 |
|---|---|---:|
| 掃描 Scan | 查 S3 bucket、CloudTrail 與 S3 Files 相關資源狀態，分辨哪些是專案資源、哪些是帳號治理資源。 | +1 |
| 比較 Compare | 比較手動 CLI、CDK deploy、CloudFormation deploy、SSM 與 SSH 的差異。 | +1 |
| 評估 Evaluate | 重新整理成本、cleanup、權限、CloudTrail 稽核與日誌誠實度風險。 | +2 |
| 驗證 Validate | 完成舊 PoC cleanup 回驗，確認新的 CloudFormation stack 已建立，並用 Console 截圖確認 Infrastructure Composer 資源關係；但 S3 Files mount 與雙向檔案同步尚待人工驗證。 | +2 |
| 報告 Report | 重寫正式日誌規則與歷史日誌，完成 CDK 部署教學書與 CloudFormation 註解整理。 | +2 |

今日總分：8 分。
硬審核後累積總分：61。
目標對齊：direct。今天主要扣回 S1-S5 主線，尤其是把新聞 PoC 變成可重做、可解釋、可在 Console 檢查的流程。

## 主管評分表自評

| 項目 | 今日證據 | 自評（1-5） | 補強方向 |
|---|---|---:|---|
| 組織認同／組織承諾 | 遵守公司帳號成本與稽核限制，沒有把 CloudTrail 當作可任意刪除的專案資源。 | 4 | 補主管或 mentor 對治理做法的確認。 |
| 盡責 | 清理舊 AWS 資源、修正分數過滿問題，並把新 PoC 的未完成驗證明確列出。 | 5 | 新 stack 驗證完後要立刻 cleanup。 |
| 團隊合作 | 依 mentor 要求，把 PoC 原理、Console 驗證與自己可重做能力放回流程。 | 4 | 補下一次和 mentor 對齊後的回饋。 |
| 創新求變 | 把 AWS 新聞驗證改成 CDK / CloudFormation 可視化流程，讓 PoC 更容易說明與重現。 | 5 | 把這段整理進 final proposal 的執行軌跡圖。 |

## Mentor / 決策

- 正式日誌寫 Cleo 做的事；AI 指令與 AI 做什麼放在 AI 執行軌跡。
- AWS PoC 不只看 CLI 成功，要手動進 Console 確認資源和檔案同步。
- CloudTrail 是帳號安全與稽核資源，不是 S3 Files cleanup 目標。
- CDK 版 PoC 可以用 CloudFormation deploy 避開 CDK bootstrap 權限限制。

## 問題與下一步

- 目前新 stack 還活著，會產生成本；做完 Console / EC2 / S3 驗證後要 cleanup。
- 需要繼續進 AWS Console 看 S3 Files file system、mount target、EC2 和 S3 object。
- 需要進 EC2 用 Session Manager 確認 `/mnt/s3files`，再做 S3 到 mount、mount 到 S3 的雙向測試。
- 17:00 automation 仍需檢查，今天是手動啟動日誌整理。

## 今日總結

今天主要把專案從「AI 可以跑」修正成「我能理解、能重做、能驗證」。S3 Files CDK PoC 已部署到 CloudFormation，且已看到 Infrastructure Composer 資源圖；完整成功還差 EC2 mount 與 S3 雙向同步驗證。
