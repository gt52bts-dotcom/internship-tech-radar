interface:
  display_name: "Skill 4 · Validate"
  short_description: "Validate candidates with a short controlled PoC gate"
  default_prompt: "Use $validate-cloud-poc to validate a candidate with built-in sandbox defaults and a short human approval gate."
