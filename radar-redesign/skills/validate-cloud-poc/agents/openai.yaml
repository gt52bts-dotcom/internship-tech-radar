interface:
  display_name: "Skill 4 · Validate"
  short_description: "Validate cloud candidates with controlled PoC gates"
  default_prompt: "Use $validate-cloud-poc to validate an evaluated candidate without bypassing approval, cost, review, or cleanup gates."
