# 每日實習日誌 - 2026-07-16（17:00 正式版）

## 今日主軸

今天完成 Cathay Tech Intel v3 在公司 AWS 帳戶的手動部署與端到端驗證。重點不只是把資源建起來，而是把 Secrets Manager、Lambda、Layer、Step Functions、S3、DynamoDB、CloudWatch Logs 串成可執行的 pipeline，並在 Anthropic API key 不可用時，改成 API-first、rubric fallback 的可降級設計，讓 demo 不會因外部金鑰或額度問題中斷。

## 今日完成成果

- 在公司 AWS 帳戶 `092211181371`、區域 `ap-southeast-1` 建立並確認 `cathay-techintel-v3/anthropic-api-key` secret，可供 Lambda 透過 ARN 讀取。
- 建立 Lambda Layer `cathay-techintel-v3-deps` version 1，並完成 7 個 Lambda function 的 zip 上傳、handler、timeout、memory、layer 與環境變數設定。
- 建立 Step Functions execution role 與 Standard state machine `cathay-techintel-v3-pipeline`，流程為 `GenerateRunId -> Task_S1 -> Task_S2 -> Task_Quote -> QuoteApproved? -> Task_S3 -> Task_S4 -> Task_S5`。
- 逐步修正第一次執行遇到的部署問題：S3 bucket 環境變數尾端空白、Lambda role 缺少 `s3:PutObject`、Step Functions logging 權限不足、Anthropic invalid API key、S5 handler 還停在 AWS 預設值。
- 修改 Lambda 共用程式，使 Anthropic 呼叫失敗時不讓 pipeline 直接中斷，而是紀錄 fallback 狀態並回到 rubric-only 評估。
- Redrive 後 Step Functions 全流程成功通過，S1/S2/Quote/S3/S4/S5 節點全綠。
- 依 HR 原始範本重新產出雙週工作週誌正式格式檔案，修正先前自行排版造成的格式不符問題。

## 驗證證據

- S1 Scan 成功輸出 `runs/2026-07-16T06-45-13.905Z/s1_scan.json`，`kept_count=29`。
- S2 Compare 成功輸出 `runs/2026-07-16T06-45-13.905Z/s2_compare.json`，`kept_count=6`。
- S2b Quote 成功輸出 `quotation.json`，結果為 `decision=approve`，估算金額 `total_usd=0.0892`，低於 `MAX_RUN_USD=0.50`。
- S3 Evaluate 與 S4 Validate 在 Anthropic key invalid 時改走 rubric fallback，避免因 401 讓 pipeline 停止。
- S5 Report 修正 handler 後成功執行，S3 已看到 `report.html` 與 `s5_report.json` 等產出。
- Step Functions 視覺化流程圖最終顯示所有主要節點成功。

## 問題與解法

- 問題：`BUCKET_NAME` 環境變數多一個尾端空白，導致 S3 bucket name validation 失敗。  
  解法：回 Lambda environment variables 修正 bucket name，重新 redrive。

- 問題：Lambda execution role 缺少對 S3 run path 的 `s3:PutObject` 權限。  
  解法：補上 Lambda execution policy，讓 S1 之後可寫入 S3。

- 問題：Step Functions 建立時 logging destination 權限不足。  
  解法：補 Step Functions role 對 CloudWatch Logs delivery/resource policy 的權限。

- 問題：Secrets Manager 中仍是 placeholder 或 invalid Anthropic key，Task_S3 呼叫 API 時回傳 `401 invalid x-api-key`。  
  解法：將 LLM 評估改為 API-first with rubric fallback，實作 `fallback_count`、`fallback_errors` 與清楚的 `mode` 標記。

- 問題：S5 Lambda handler 沿用 AWS 預設 `lambda_function.lambda_handler`。  
  解法：改為 `s5_report.handler` 後 redrive 成功。

## Skill 分數

| 工作成果 | 對應 Skill | 今日分數 | 目標對齊 | 證據 |
|---|---:|---:|---|---|
| 公司 AWS 帳戶完成 S1/S2/Quote/S3/S4/S5 Step Functions 端到端成功 | Skill 5 Report | +8 | direct | Step Functions 全綠，S3 產出 report 與 JSON |
| 將 Anthropic invalid key 失敗改為 API-first fallback，保留 rubric-only 可執行路徑 | Skill 3 Evaluate / Skill 4 Validate | +6 | direct | S3/S4 redrive 後通過，mode 能標示 fallback |
| 修正 IAM、S3、Step Functions logging、Lambda handler 等部署驗證問題 | Skill 4 Validate | +4 | supporting | 多個部署錯誤皆有 cause、fix、redrive 驗證 |
| 依 HR 原始範本重做雙週工作週誌 | Skill 5 Report | +3 | supporting | 產出格式正確版 docx，修正表格結構 |

今日總分：21 分。這是直接對齊專案 demo 與最終提案的高權重進展，因為今天把「可在公司環境跑通」從假設變成已驗證事實。

## 今日學習

今天最大的學習是：雲端部署的價值不只在建立資源，而在每一步都能被驗證、被追溯、被修復。Step Functions 的紅色節點、Event history、Lambda logs、S3 object、DynamoDB log 都是 checkpoint。這也呼應 mentor 提醒的 AI PM 要嚴格：做之前先定義 checkpoint，做完每一步都要有驗證機制，不能只靠「好像成功」。

## 明日與後續檢核點

- 確認 `report.html` 的開啟方式：若 S3 object URL 因私有 bucket 無法直開，改用 Download 或 presigned open。
- 檢查 DynamoDB `cathay-techintel-v3-picks-log` 是否已有 actor=`ai` 的 pick log。
- 若要驗證真正 Claude API 路徑，再補正式 Anthropic API key 或評估改用 AWS Bedrock。
- 將今天的成果整理進 demo checklist 與 final proposal 執行軌跡。
- 持續維護每日 17:00 AI PM 流程：Git、Notion、dashboard 三方要同步，且只記錄有證據的完成事項。
