# AI 執行軌跡 - 2026-07-23

## 07:00-07:59｜S3 Files 指定新聞完整雷達與 PoC

- 使用者指令：以已手動部署的整套技術雷達，從指定 AWS News Blog 的 S3 Files 文章完成評分、CloudFormation PoC 到最後報告；CDK bootstrap role 若失敗要自行切換方法。
- AI 判斷：既有 RSS S1 不會保證收錄數月前文章，若直接執行會變成不相干的今日新聞。新增受控 `seed_article` 分支，把指定文章明確標為手動輸入；保留一般 RSS 路徑，避免偽稱 RSS 掃到文章。
- 實際處理：確認手動雷達 stack 與 S1-S5 資源均完成；更新既有 CloudFormation stack 的 Lambda code 和 Step Functions 定義，使用指定文章啟動 S1→S5。第一次 Step Functions 輸入因 UTF-8 BOM 被拒絕，改成 UTF-8 無 BOM 後成功。
- 驗證證據：Step Functions `SUCCEEDED`；quote gate USD 0.0232 並核准，但 S3/S4 因 Anthropic 呼叫失敗走 rubric-only fallback；最終只有單一候選，平均 4.35，human review 仍待人工決定。
- PoC：重用既有 S3 Files CloudFormation stack，確認 EC2/SSM 與 nfs4 mount；S3 API→mount 可讀，mount→S3 約 36 秒後可讀回。未新增第二套資源。
- 待接續：Cleo 到 Console 人工確認 CloudFormation、S3 Files、EC2 與 S3 object；完成證據保存後 cleanup S3 Files PoC stack，並由真人處理 review gate。

## 08:00-08:59｜LLM fallback 根因與 layer 修正

- AI 判斷：quote gate 已核准卻仍落入 rubric fallback，必須區分程式相容性、API key、網路與費用問題，不能把 fallback 當成 LLM 評分成功。
- 實際處理：讀取 pipeline JSON 的 fallback error，確認第一個阻礙是 layer 缺少 Linux/Python 3.12 的 `pydantic_core` 原生模組；以 Linux wheel 重建 Anthropic layer，更新既有 CloudFormation stack，再重跑相同新聞。
- 驗證證據：第二次 Step Functions 仍 `SUCCEEDED`，代表 layer 相容性問題已排除；新的 fallback error 是 Anthropic API 401 invalid key。沒有讀取、輸出或保存 secret 值。
- 待接續：由持有核准 API key 的人更新 Secrets Manager 後重跑；完成 Console 證據保存後 cleanup 既有 S3 Files PoC stack。

## 08:00-08:59｜指定新聞的 PoC 審查閘門

- 使用者指令：可以保留直接輸入指定新聞的端口，但它也必須先經評分確認適合落地才能開始 PoC。
- AI 判斷：直接輸入只能是 S1 入口，不能繞過 S2-S5，也不能由雷達自動建立付費資源。需要把 PoC 資格明確輸出給人類，而非只留在文字說明。
- 實際處理：在 S5 加入 `poc_gate`。條件為完整 S1-S5、平均分至少 3.75、證據信心至少 medium、沒有治理旗標；符合時只會得到 `eligible_for_human_poc_review`，`automatic_poc_start` 永遠為 false。
- 驗證證據：本機 unit-style test 通過；既有 CloudFormation stack 更新完成後，指定新聞實跑的 Step Functions 為 `SUCCEEDED`，回傳 `awaiting_human_poc_review`、eligible count 1、automatic start false。
- 待接續：若使用者在 GUI 加入輸入視窗，應把 `poc_gate` 顯示為「可送人工審查」或「不可做 PoC」，並要求填入人工核准理由。

## 08:00-08:59｜下週二 AI PM 科會報告需求

- 使用者指令：下週二要做 10 分鐘 AI PM 科會報告，要求展示 input/output 差異、過去記憶串聯、AI 對人類的幫助、共融活動、主動反問與待辦機制、spec-driven 邊界。
- AI 判斷：這不是 AI 功能導覽，而是要以可證明的協作前後差異展示 AI PM 如何幫助人類；也必須揭露錯誤、限制與人類決策責任。
- 實際處理：將其寫入長期專案記憶與當日 AI PM 待辦；暫不直接製作簡報，先保留需選擇的證據與報告對象。
- 待接續：整理 7 至 8 頁、每頁約 1 分鐘的簡報結構，選擇去識別化 input/output 截圖與 demo 範例。

## 09:00-09:59｜AI PM 科會簡報視覺參考確認

- 使用者指令：提供既有實習專案匯報 PDF 作為類似模板，晚點要協助製作 AI PM 簡報；內容需完全不同、封面不放照片、重新設計。
- AI 判斷：範本適合借用企業感、高留白、單頁單訊息、卡片和流程圖的視覺節奏，但不應沿用原本的個人介紹、技術雷達主題或照片構圖。
- 實際處理：讀取 PDF 的 10 頁文字結構並渲染檢視封面、問題定義、人機角色與代理型組織頁面；已把可借鑑與不可沿用的邊界寫入 AI PM 暫存與長期記憶。
- 待接續：製作時以「AI PM 如何讓人類工作可追溯、可決策」為主軸，準備無照片封面與去識別化的 input/output 證據頁。

## 10:00-10:59｜AI PM 科會簡報初稿

- 使用者指令：確認現在就開始做簡報，詢問是否應先做 PoC。
- AI 判斷：PoC 的下一步需要 Console 人工確認及有效 LLM key，無法在背景自動完成；因科會時程明確，優先完成可展示的簡報，並在內容誠實呈現 PoC 的驗證與待處理邊界。
- 實際處理：新增 8 頁無照片 PPTX，使用企業感、高留白與流程節奏，但不沿用參考檔原內容。頁面涵蓋 input/output、事件串聯、AI PM 實際幫助、共融活動情境、主動反問、spec-driven、human review gate 和結論。
- 驗證證據：使用 artifact-tool 匯出每一頁 PNG 並逐頁視覺檢視，修正第 6 頁標題與副標間距；沒有發現裁切或非預期重疊。外部 PPTX render/overflow 工具因 Windows cp950 編碼錯誤無法執行，未將其寫成通過。
- 待接續：使用者可先看初稿，下一輪再依科會對象與想放的 input/output 截圖調整；PoC 則保留給 Console 人工確認、key 修正重跑與 cleanup。

## 11:00-11:59｜AI PM 科會簡報改為協作校正主線

- 使用者回饋：初稿太沒重點，要求仔細回顧長期一起工作期間的實際幫助、校正與調整。
- AI 判斷：原稿把 AI PM 抽象化成一般功能，沒有把使用者的回饋如何改變日誌、計分、PoC 驗證、誠實揭露與待辦行為呈現出來；這才是科會報告最有說服力的事實。
- 實際處理：保留舊檔，另存重做協作校正版本。用五個可查證的校正案例取代泛用功能介紹：日誌重寫、107→53 嚴格重算、可重做的 PoC、fallback／待驗證揭露、主動反問與情境待辦。
- 驗證證據：逐頁檢視 8 張匯出 PNG，發現第 2、3、5、6 頁長標題與副標重疊後已縮短標題並重匯出；複查畫面未見裁切或非預期重疊。外部 PPTX render/overflow 工具的 Windows 編碼限制仍存在，未宣稱該工具通過。

## 12:00-12:59｜PoC 真人審查閘門 Console 位置核對

- 使用者指令：暫停簡報，先做 PoC，詢問真人審查閘門在哪裡、Console 要去哪找。
- AI 判斷：目前並沒有已部署的 GUI 審查頁；human review 是後端 Lambda 審查紀錄，必須先從 Step Functions 確認候選與 gate，再由真人在 Lambda Console 明確送出決定。這個決定不能被描述為自動開始 PoC。
- 實際處理：以唯讀 AWS CLI 核對 stack 更新完成、列出 state machine 與 Lambda 名稱；讀取 `record_human_pick.py` 確認必填欄位與 approve 時的候選 ID 限制。
- 驗證證據：`cathay-techintel-v3-cfn-pipeline`、`cathay-techintel-v3-cfn-recordhumanpick` 均存在；stack 為 `UPDATE_COMPLETE`。尚未以人類身分送出 approve/reject，也未建立新 PoC 資源。

## 12:00-12:59｜S3 Files execution 與 gate 對應更正

- 使用者回饋：在 S3 Files execution 圖形頁找不到先前所說的 `poc_gate`。
- AI 判斷與更正：先前把後續 gate 測試 execution 的結果，錯套到早於 gate 整合完成的 S3 Files 指定新聞 execution；這是引導不精確，不應要求使用者在不存在的欄位中尋找。
- 唯讀驗證：列出 pipeline 最近 executions 並逐筆讀取 S5 output。`s3files-news-20260723-0750`、`s3files-news-20260723-0800` 沒有 `poc_gate`；`poc-gate-check-20260723-0830` 才有 `awaiting_human_poc_review` 與一筆測試候選。
- 正確下一步：以目前已更新的 pipeline 重跑指定 S3 Files 新聞，確保該新聞自己的 S5 output 產生 `poc_gate` 後，再由真人在 review Lambda 記錄核准／拒絕與理由；gate test 候選不可作為 S3 Files PoC 依據。

## 13:00-13:59｜S3 Files 指定新聞重跑取得可審查 gate

- 實際處理：以既有 seed article input 重新啟動現行 pipeline。第一次讀取歷史 execution 時多筆 ARN 被 PowerShell 合併，AWS 拒絕請求；第二次因命令列 JSON 引號被破壞，AWS 拒絕 execution input。兩次都未啟動 execution、未建立資源。改用 `file://` JSON 輸入後成功啟動。
- 驗證證據：`s3files-news-20260723-gate` 為 `SUCCEEDED`；S5 回傳 `awaiting_human_poc_review`、候選 `M-2E486BFB`、`automatic_poc_start=false`。這是 S3 Files 新聞本身的 gate，不是先前的獨立 gate test。
- 待接續：由 Cleo 在 review Lambda 記錄 approve/reject、理由、成本上限、成功標準與 cleanup；此決定不會自動建立 PoC 資源，下一步仍需另行確認 PoC 範圍。
